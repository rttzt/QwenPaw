- **异步 HTTP 客户端**：基于 `openai.AsyncOpenAI`、`anthropic.AsyncAnthropic` 及 `google.genai` 实现各平台异步通信。
- **数据校验**：使用 `pydantic` BaseModel 进行 `ProviderInfo` 和 `ModelInfo` 的结构化定义与序列化。
- **并发原语**：利用 `asyncio.Semaphore` 和 `asyncio.Lock` 构建线程安全的限流器与单例初始化逻辑。