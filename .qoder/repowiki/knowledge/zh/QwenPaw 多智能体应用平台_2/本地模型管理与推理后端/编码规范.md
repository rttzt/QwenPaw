- 采用进程级隔离执行耗时或阻塞任务（如模型下载、文件解压），通过 `ProcessDownloadTaskSpec` 定义任务规范，并利用 `Queue` 进行跨进程进度通信。
- 使用 `asyncio.Lock` 保护共享资源的状态变更（如服务器启停、配置更新），确保并发场景下的状态一致性。
- 遵循“预检-执行-清理”的资源管理模式，在启动下载或服务前检查环境兼容性（如 macOS 版本、磁盘空间），并在任务结束或异常时执行自动清理。