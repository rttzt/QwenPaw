- 所有涉及阻塞 I/O 的核心逻辑（如 ZIP 读写、文件移动）均封装在同步函数中，并通过 `asyncio.to_thread` 暴露为异步接口。
- 在执行目录原子替换前，必须先调用 `cleanup_stale_restore_artifacts` 清理可能因上次崩溃遗留的 `.restore_tmp` 或 `.restore_old` 目录。
- 备份元数据 (`meta.json`) 始终存储在 ZIP 根目录，并在压缩完成后通过 `writestr` 写入，作为备份有效性的校验依据。