- 采用分层架构：`architecture.py` 定义数据模型（Manifest/Record），`loader.py` 负责基于 `plugin.json` 的发现与动态模块导入，`registry.py` 作为单例中心管理 Provider 和 Hook 注册，`api.py` 暴露给插件开发者进行能力注册。
- 依赖方向明确：Loader 依赖 Registry 和 API，API 反向持有 Registry 引用以完成注册回调。
- 入口点：`__init__.py` 导出核心类，外部通过 `PluginLoader` 启动加载流程。