- 采用增量流式处理模式，在 `server.py` 和 `client.py` 中均通过追踪前缀状态（如 `_prev_text`）计算 delta 以适配 ACP 协议。
- 统一使用自定义的层次化异常类（如 `ACPConfigurationError`, `ACPSessionError`）继承自基类 `ACPErrors` 进行错误分类。
- 在权限处理中实施双重校验机制，既检查预定义的硬编码危险命令模式，又校验文件路径是否超出工作目录（cwd）范围。