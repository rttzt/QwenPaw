- 采用“控制器-执行者-验证者”三元架构，Master 仅负责任务分发，具体编码由后台 Worker 会话完成。
- 在 Phase 2 执行循环中强制禁用 Master 的实现类工具（如 edit_file），确保其仅作为编排者存在。
- 引入对抗性验证机制，Verifier 必须运行实际命令并输出结构化 VERDICT 才能判定 Story 通过。
- 使用优先级（priority）字段控制任务并行度，相同优先级的 Story 会被批量分发给多个 Worker 同时执行。