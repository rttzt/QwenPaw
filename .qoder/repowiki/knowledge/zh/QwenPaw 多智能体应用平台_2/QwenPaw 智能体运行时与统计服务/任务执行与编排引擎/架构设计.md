- **入口层**：`handler.py` 负责解析 `/mission` 指令并初始化隔离的 Loop 目录结构。
- **核心引擎**：`mission_runner.py` 管理两阶段生命周期（Phase 1 PRD生成/确认，Phase 2 受限工具的执行循环）。
- **状态管理**：`state.py` 维护 `prd.json`、`loop_config.json` 等持久化文件，支持 Git 环境探测。
- **提示词工厂**：`prompts.py` 动态构建 Master（控制器）、Worker（执行者）及 Verifier（验证者）的系统提示词。