- **LibreOffice 宏配置**：首次运行 XLSX 公式重算时，`scripts/recalc.py` 会自动在用户配置目录安装 `RecalculateAndSave` Basic 宏。
- **沙箱兼容层**：在受限环境中，`scripts/office/soffice.py` 会自动编译并注入 `lo_socket_shim.so` 以允许 LibreOffice 正常启动。
- **执行方式**：所有脚本需在该技能目录下运行，例如 `cd src/qwenpaw/agents/skills/docx-en && python scripts/office/unpack.py document.docx unpacked/`。