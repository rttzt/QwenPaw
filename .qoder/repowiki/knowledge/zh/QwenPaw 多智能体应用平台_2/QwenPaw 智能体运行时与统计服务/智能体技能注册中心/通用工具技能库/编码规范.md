- 每个技能模块根目录下仅包含一个名为 `SKILL.md` 的标准定义文件，作为技能的唯一入口和描述载体。
- 技能元数据统一采用 YAML Frontmatter 格式，必须包含 `name`、`description` 及 `metadata`（含版本号与图标）字段。
- 涉及多语言支持的技能，采用“功能名-语言代码”（如 `guidance-en`、`guidance-zh`）的目录命名规范以实现版本隔离。