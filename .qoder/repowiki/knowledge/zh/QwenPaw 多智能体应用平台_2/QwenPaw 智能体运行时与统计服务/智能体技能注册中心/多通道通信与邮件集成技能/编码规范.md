- 所有技能文档均采用 YAML Frontmatter 定义 `name`、`description` 及 `metadata`（包含版本号、emoji 标识及二进制依赖要求）。
- 在涉及敏感凭据（如 Client Secret、邮箱密码）的配置场景中，统一禁止 Agent 自动修改本地配置文件，转而引导用户通过控制台或手动编辑完成。
- 自动化工作流中强制要求在执行关键状态变更（如发送消息、发布应用）前先进行上下文查询（如 `chats list`、页面快照），以避免参数猜测导致的错误。