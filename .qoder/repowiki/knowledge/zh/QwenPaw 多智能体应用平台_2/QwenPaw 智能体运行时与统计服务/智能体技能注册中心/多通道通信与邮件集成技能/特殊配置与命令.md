- **Himalaya 初始化**：需执行 `himalaya account configure <name>` 交互式生成 `~/.config/himalaya/config.toml`，并建议使用 `pass` 或系统 keyring 安全存储密码。
- **钉钉自动化前置确认**：在执行钉钉渠道绑定时，必须先向用户确认应用名称、描述及图标规格，并在遇到登录页时暂停等待人工介入。
- **163邮箱特殊配置**：使用 163 邮箱时需在配置中开启 `backend.extensions.id.send-after-auth = true` 并预先创建 `Sent` 文件夹。