- 所有跨智能体通信消息必须携带身份前缀 `[Agent <id> requesting]` 以明确来源并防止循环调用。
- 在发起任何协作或规划请求前，必须先调用 `list_agents()` 或 `qwenpaw agents list` 获取目标智能体的准确 ID，禁止硬编码或猜测 ID。
- 多轮对话中必须复用首次交互返回的 `session_id`（或 `--session-id`），以确保上下文连续性并避免创建冗余会话。
- 后台任务提交后禁止高频轮询，应根据任务复杂度设置合理的查询间隔（如简单任务 10s+，复杂任务 30s+），或在处理其他工作后异步查询。