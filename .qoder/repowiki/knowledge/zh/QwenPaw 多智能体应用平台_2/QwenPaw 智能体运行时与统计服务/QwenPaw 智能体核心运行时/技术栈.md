- **AgentScope Runtime**: 基于 `agentscope.agent.ReActAgent` 扩展，利用其 Hook 系统（`pre_reasoning`, `post_acting` 等）实现生命周期管理。
- **多模态适配**: 在 `model_factory.py` 中实现针对 Anthropic/Gemini/OpenAI 不同 API 规范的消息格式化器，处理 Base64 媒体块和视频占位符。
- **技能元数据**: 使用 `python-frontmatter` 解析 `SKILL.md` 中的 YAML 头信息，管理技能版本与依赖。