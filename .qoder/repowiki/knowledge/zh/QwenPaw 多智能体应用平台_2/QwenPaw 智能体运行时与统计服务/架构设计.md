- **核心与扩展解耦**：`agents` 模块采用懒加载机制（`__getattr__`）隔离重型依赖，确保 `skills_registry` 等子模块可独立初始化。
- **标准化能力契约**：`skills_registry` 通过 `SKILL.md` 定义原子能力，由 `agent_core` 动态注册并注入 `react_agent` 执行流。
- **上下文与记忆协同**：`context_memory` 提供动态压缩与长期记忆检索，支撑 `mission_execution` 的复杂任务迭代。
- **观测侧独立演进**：`agent_stats` 作为独立包存在，通过共享的数据模型与工作区文件结构与主运行时保持松耦合，提供非侵入式统计能力。