- 采用基于语言（en/zh/ru）和环境（local/qa）的目录分层结构，实现模板的国际化与环境隔离。
- 核心架构由六类标准文档构成：`AGENTS.md`（行为准则）、`SOUL.md`（核心价值观）、`BOOTSTRAP.md`（初始化流程）、`PROFILE.md`（身份与用户画像）、`MEMORY.md`（工具配置与经验沉淀）以及 `HEARTBEAT.md`（周期性任务策略）。
- 模块通过 Frontmatter 元数据（如 `read_when`）定义模板的触发时机，作为 Agent 运行时加载上下文的结构化依据。