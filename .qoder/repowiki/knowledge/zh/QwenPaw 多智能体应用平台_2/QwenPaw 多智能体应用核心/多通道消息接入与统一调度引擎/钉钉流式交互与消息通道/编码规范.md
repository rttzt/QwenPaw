- 采用“早期 ACK”机制（`_ack_early`），在流式回复开始前立即 resolve `reply_future` 以防止钉钉服务端因超时而重试回调。
- 统一使用 `message_id` 进行线程安全的去重处理，确保在钉钉网络抖动导致的重复投递场景下仅执行一次业务逻辑。
- 对会话 Webhook 实行“内存+磁盘”双层存储策略，并在发送失败或检测到过期时间临近时主动触发失效清理与 Open API 降级。
- 在构建 AI 卡片或 Markdown 消息时，通过正则工具模块（`markdown.py`）自动修复列表间距与代码块缩进，以适配钉钉客户端的渲染特性。