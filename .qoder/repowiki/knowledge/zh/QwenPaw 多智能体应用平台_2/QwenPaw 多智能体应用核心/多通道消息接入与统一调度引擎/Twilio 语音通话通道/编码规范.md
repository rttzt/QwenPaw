- 所有与外部系统（Twilio API、Tunnel 进程）的同步或阻塞操作均通过 `asyncio` 包装为异步方法，以保持通道主循环的非阻塞特性。
- 采用一次性令牌（One-time token）机制保护 WebSocket 接入点，防止未授权的直接连接尝试。
- 在 WebSocket 消息处理中严格区分事件类型（setup, prompt, interrupt），并在 `finally` 块中确保会话状态清理与连接关闭。