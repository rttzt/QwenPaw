- **SIP/RTP 实现**：`pyVoIP`（开发模式）与 `livekit-api` / `livekit-rtc`（生产模式）。
- **语音服务**：阿里云 DashScope SDK（`dashscope` 用于 TTS，`dashscope_realtime` 用于流式 STT）。
- **兼容性处理**：通过 `_audioop_compat.py` 提供 Python 3.13+ 对已移除的 `audioop` 模块的兼容支持（依赖 `audioop-lts`）。