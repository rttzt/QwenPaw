- 采用异步流式处理（AsyncIterator）实现 TTS 音频生成与播放，以降低首字延迟并支持随时中断（Barge-in）。
- 所有底层同步阻塞操作（如 pyVoIP 的音频读写、DashScope 的同步调用）均通过 `asyncio.to_thread` 或后台线程池封装，确保主事件循环不被阻塞。
- 使用 `asyncio.Queue` 作为音频数据在不同协程任务（如音频读取器与 STT 引擎）之间传递的标准缓冲机制。