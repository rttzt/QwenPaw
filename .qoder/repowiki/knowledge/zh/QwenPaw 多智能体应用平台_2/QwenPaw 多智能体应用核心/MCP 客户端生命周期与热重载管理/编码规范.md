- 所有对共享客户端字典的读写均通过 `asyncio.Lock` 保护，且尽量缩小临界区，将耗时连接操作置于锁外执行。
- 状态化客户端将 `AsyncExitStack` 与 `ClientSession` 的进入/退出限定在同一后台任务中，通过事件（ready/reload/stop）驱动生命周期切换。
- 配置热重载采用“mtime + 配置哈希”双检机制，并在独立后台任务中执行增量替换，避免阻塞轮询主循环。
- 对可能失败的连接与关闭操作统一包裹超时与异常捕获，并在失败路径中强制执行资源清理与状态复位。