- 服务组件采用声明式注册模式，通过 `ServiceDescriptor` 定义类、初始化参数、启动/停止方法及优先级，而非在构造函数中硬编码实例化逻辑。
- 异步启动过程中，所有同步的组件构造函数均通过 `asyncio.to_thread` 卸载至线程池执行，以防止阻塞事件循环影响 HTTP 请求响应。
- 工厂函数（Factory Functions）统一接收 `Workspace` 实例和可选的已有服务实例作为参数，负责处理新创建与复用场景下的组件连线（Wiring）逻辑。