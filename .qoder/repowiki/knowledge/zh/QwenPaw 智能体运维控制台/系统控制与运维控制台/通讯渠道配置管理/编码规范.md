- 采用自定义 Hooks（如 `useChannels`, `useChannelQrcode`）将数据获取、状态管理与 UI 渲染解耦。
- 在 `ChannelDrawer` 中利用 Ant Design Form 的 `shouldUpdate` 属性实现表单字段的动态显隐与联动校验。
- 通过 `components/index.ts` 建立统一的模块导出边界，外部仅通过该入口引用子组件与工具函数。
- 使用 `React.memo` 对高频渲染的展示型组件（如 `ChannelCard`）进行性能优化。