- 采用 React 函数组件结合 Ant Design Form 实现配置表单，通过 `useAgentStore` 监听代理变化自动刷新配置。
- 内置 `parseEvery` 和 `serializeEvery` 工具函数处理时间间隔字符串（如 '6h'）与表单数值的双向转换。
- 使用 CSS Modules (`index.module.less`) 进行样式隔离，并适配深色模式。
- 依赖 `@agentscope-ai/design` 和 `antd` 组件库，通过 `api` 模块与后端交互。