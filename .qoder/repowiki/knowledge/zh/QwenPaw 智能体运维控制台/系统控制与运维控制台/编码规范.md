- 采用 `useXxx` 自定义 Hook 封装数据获取与业务逻辑，实现视图层与逻辑层分离。
- 使用 `index.module.less` 进行模块化样式管理，避免全局样式污染。
- 通过 `components/` 目录聚合页面级子组件（如 Drawer, Columns, Constants），保持主文件简洁。
- 统一使用 `@agentscope-ai/design` 组件库构建界面，确保设计语言一致。