- 使用自定义 Hook（如 `useSessions`）将 API 调用、状态管理与业务逻辑从 UI 组件中剥离。
- 在 `constants.ts` 中统一处理 ISO 时间戳的时区标准化（补全 'Z'），确保前端显示与排序的 UTC 一致性。
- 通过 `components/index.ts` 建立统一的导出层，简化父组件对子模块内部实现的依赖。
- 表格列配置采用工厂函数 `createColumns` 模式，通过参数注入事件处理器以解耦视图与交互逻辑。