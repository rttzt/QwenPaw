- 使用 `parseErrorDetail` 统一解析 API 错误，并针对 `conflict` 或 `suggested_name` 等特定 reason 触发二次确认或重命名流程。
- 在技能创建、导入或删除等操作成功后，调用 `invalidateSkillCache` 清除缓存并强制重新加载数据以保持状态同步。
- 通过 `localStorage` 持久化存储内置技能通知的指纹（fingerprint），用于判断用户是否已查看过最新的更新提示。