- **逻辑层**：采用自定义 Hook `useSkillPool` 集中管理状态与业务逻辑，处理 API 交互（如 `api.listSkillPoolSkills`、`api.downloadSkillPoolSkill`）及冲突解决。
- **表现层**：主组件 `index.tsx` 负责页面布局与视图切换（卡片/列表），集成渐进式渲染 Hook `useProgressiveRender` 优化大数据量展示。
- **组件层**：`components` 目录封装了专用模态框（`BroadcastModal` 用于分发，`ImportBuiltinModal` 用于内置同步）及编辑抽屉 `PoolSkillDrawer`，通过 `index.ts` 统一导出。
- **辅助模块**：`builtinNotice.ts` 独立处理内置技能更新通知的文本格式化逻辑。