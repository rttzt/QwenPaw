- 采用自定义 Hook `useEnvVars` 封装数据获取逻辑，实现视图与状态管理的解耦。
- 页面主组件 `index.tsx` 承担核心业务逻辑，包括本地脏数据追踪（dirty state）、行级索引维护及表单校验。
- 交互模式混合了即时生效（单行删除调用 API）与批量提交（新增/修改后统一 Save），通过 `isNew` 标记区分数据状态。
- UI 层拆分为原子化组件（`EnvRow`, `Toolbar` 等），通过 `components/index.ts` 统一导出，降低主文件复杂度。