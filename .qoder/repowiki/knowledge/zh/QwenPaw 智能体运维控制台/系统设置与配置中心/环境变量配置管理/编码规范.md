- 敏感字段（Value）默认以密码形式展示，并提供显隐切换按钮以增强安全性。
- 已持久化的变量键名（Key）在编辑模式下被禁用，仅允许修改值或整行删除，防止关键标识符变更引发不一致。
- 执行删除操作前必须通过 `Modal.confirm` 进行二次确认，且确认框需包含具体的变量名称以明确操作对象。
- 本地校验逻辑涵盖非空检查、正则格式匹配（`^[A-Za-z_][A-Za-z0-9_]*$`）以及键名唯一性检查。