- 使用自定义 Hook（如 `useToolGuard`）集中处理 API 调用、本地状态管理及业务逻辑，保持 UI 组件纯净。
- 在表格或列表渲染中，根据数据来源（builtin/custom）或状态（disabled）动态调整 UI 样式（如透明度、标签颜色）。
- 采用 `components/index.ts` 作为统一导出入口，简化页面组件的引入路径并隐藏内部实现细节。
- 在处理多行文本输入（如正则表达式）时，使用 `split('\n')` 和 `filter(Boolean)` 将其转换为数组格式提交给后端。