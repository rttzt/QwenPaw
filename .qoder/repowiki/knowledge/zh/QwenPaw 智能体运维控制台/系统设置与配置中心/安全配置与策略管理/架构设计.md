- **分层架构**：采用 `Page (index.tsx)` -> `Custom Hooks (useToolGuard, useSkillScanner)` -> `UI Components` -> `API Layer` 的分层设计。
- **状态管理**：通过自定义 Hook 封装业务逻辑与远程状态同步（如 `useToolGuard` 管理内置/自定义规则合并），实现视图与逻辑解耦。
- **组件化设计**：将复杂表单拆分为 `RuleTable`、`FileGuardSection` 等独立组件，并通过回调函数（如 `onSave`）与父级页面进行状态交互。
- **接口边界**：依赖 `console/src/api/modules/security.ts` 定义的类型契约与请求方法，确保前后端数据交互的一致性。