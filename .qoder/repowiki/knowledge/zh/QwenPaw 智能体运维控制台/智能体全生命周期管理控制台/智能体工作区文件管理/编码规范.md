- 所有 UI 文案均通过 `useTranslation` Hook 配合 `t()` 函数实现国际化，避免硬编码字符串。
- 文件操作反馈统一使用 `useAppMessage` Hook 提供的 `message.success` 或 `message.error` 进行提示。
- 在 `useAgentsData` 中处理异步数据获取时，均采用 try-catch 包裹 API 调用并捕获错误日志。
- 样式定义统一采用 CSS Modules 方案，通过 `import styles from './index.module.less'` 引入并动态绑定类名。