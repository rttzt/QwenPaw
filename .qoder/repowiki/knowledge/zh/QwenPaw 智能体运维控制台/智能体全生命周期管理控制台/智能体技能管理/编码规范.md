- 采用“乐观更新 + 异常回滚”或“即时刷新”策略，在执行启用/禁用、删除等变更后立即调用 `refreshSkills` 或手动更新本地 State。
- 在创建、上传或导入技能时，若检测到名称冲突，统一通过 `showConflictRenameModal` 触发重命名流程，而非直接覆盖。
- 所有涉及技能内容修改的操作（如创建、编辑、优化）均包含 YAML Frontmatter 校验逻辑，确保 `name` 和 `description` 字段存在。
- 使用 `parseErrorDetail` 统一解析后端返回的错误结构，针对 `conflict`、`security_scan_failed` 等特定 reason 进行差异化 UI 反馈。