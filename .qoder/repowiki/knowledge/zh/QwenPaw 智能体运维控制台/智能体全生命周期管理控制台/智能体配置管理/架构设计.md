- **逻辑分层**：采用容器-组件模式，`index.tsx` 作为页面容器负责 Tab 路由与状态聚合，`useAgentConfig.tsx` 封装 API 交互与表单生命周期。
- **动态渲染**：基于 `CONTEXT_MANAGER_BACKEND_MAPPINGS` 等常量映射表，根据当前选中的后端类型（如 light/remelight）动态注入对应的配置卡片组件。
- **数据流**：通过 Ant Design Form 实例实现跨组件的表单状态共享，利用 `Form.useWatch` 在子组件中响应式地获取关联字段值以进行联动校验或计算。