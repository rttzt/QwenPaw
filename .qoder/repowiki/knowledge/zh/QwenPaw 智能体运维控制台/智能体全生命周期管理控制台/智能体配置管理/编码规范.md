- 所有配置项均封装为独立的 Card 组件，并通过 `components/index.ts` 统一导出，保持模块入口整洁。
- 复杂嵌套对象字段（如 `light_context_config`）在 Form.Item 中使用数组路径（name={["key", "subKey"]}）进行绑定。
- 使用 `Form.useWatch` 钩子在组件内部监听特定字段变化，以实现动态阈值计算或字段间的联动禁用逻辑。