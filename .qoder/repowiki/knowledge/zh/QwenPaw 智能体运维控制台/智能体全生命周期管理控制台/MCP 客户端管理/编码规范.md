- 使用自定义 Hook (如 `useMCP`) 集中管理组件的业务逻辑与副作用，保持视图组件纯净。
- 通过 `React.memo` 优化高频渲染的列表项组件（如 `MCPClientCard`），避免不必要的重渲染。
- 采用 JSON 文本域直接编辑复杂配置对象，并提供多格式（嵌套/扁平）解析兼容逻辑。
- 利用 `useTranslation` 钩子实现全模块界面的国际化支持，确保文案可配置。