- **视图层**：`index.tsx` 作为页面入口，基于 `@agentscope-ai/design` 的 Card 组件渲染工具网格，并通过 `PageHeader` 集成全局开关。
- **逻辑层**：`useTools.ts` 封装核心业务逻辑，采用乐观更新（Optimistic Update）策略处理 `toggleEnabled` 和 `enableAll/disableAll` 等异步操作，并在失败时自动回滚状态。
- **数据层**：依赖 `api/modules/tools` 定义的接口契约，通过 RESTful API 与后端交互，实现工具状态的持久化同步。