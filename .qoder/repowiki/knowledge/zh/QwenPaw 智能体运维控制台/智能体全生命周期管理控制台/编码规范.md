- 采用“页面入口 + 业务逻辑 Hook + 展示组件”的分层架构，实现 UI 与逻辑解耦（如 Skills 页面的 useSkillsPage）。
- 统一使用 `PageHeader` 组件构建页面顶部结构，确保面包屑导航、标题及操作按钮在不同子模块间保持视觉一致。
- 在涉及复杂表单或异步数据获取的场景下，普遍采用自定义 Hooks（如 useAgentConfig, useMCP）封装状态管理与副作用。