- 采用 API 模块与类型定义一一对应的组织方式（如 `api/modules/agent.ts` 对应 `api/types/agent.ts`）。
- 页面级组件普遍采用自定义 Hook（如 `useAgents`, `useSkills`）抽离业务逻辑，实现视图与状态的解耦。
- 样式文件统一使用 CSS Modules（`.module.less`）进行局部作用域隔离，避免全局样式冲突。