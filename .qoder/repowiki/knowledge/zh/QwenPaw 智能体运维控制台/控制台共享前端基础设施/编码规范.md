- API 模块采用按业务域拆分文件（如 `modules/chat.ts`），并在 `api/index.ts` 中统一导出聚合对象。
- UI 组件普遍采用 CSS Modules（`.module.less`）进行样式隔离，避免全局样式污染。
- 页面级组件在路由配置中统一使用 `lazyImportWithRetry` 进行异步加载，以增强网络不稳定时的容错性。
- 自定义 Hook（如 `useAppMessage`）用于封装第三方库（Ant Design）的上下文访问，确保与 ConfigProvider 的配置保持一致。